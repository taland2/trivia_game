// Dart codegen stub (doc 15 Phase 0; full codegen lands in Phase 1 alongside the
// Flutter app). Will emit Dart request/response classes from the zod schemas into
// app/lib/api_contract/ — the single source of truth stays in src/.
//
// CI's codegen-drift check (doc 13 §2 item 3) activates once this emits real code.
console.log(
  "[api-contract] Dart codegen not implemented yet (Phase 1 — requires app/ to exist).",
);
